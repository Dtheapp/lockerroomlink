// =============================================================================
// DESIGN STUDIO - World-class flyer designer
// This file now wraps the new DesignStudioPro component
// =============================================================================

import React from 'react';
import { DesignStudioPro } from './design-studio';

const DesignStudio: React.FC = () => {
  return <DesignStudioPro />;
};

export default DesignStudio;
